export function Nav() {
  return (
    <nav>
      {/* <!-- Appears only when we are in a room --> */}
      <button class="btn-exit">End Room</button>
      {/* <!-- Appears always --> */}
      <h2>songBuddy</h2>
    </nav>
  );
}
